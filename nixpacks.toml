[phases.build]
cmds = ["cd frontend && npm install && npm run build && rm -rf ../backend/static && cp -r dist ../backend/static"]

[start]
cmd = "cd backend && uvicorn app.main:app --host 0.0.0.0 --port $PORT"
